import React from 'react';
import PropTypes from 'prop-types';

const Card = (props) => {
  return (
    <article className='card'>
      <h4>I am a card</h4>
    </article>
    )
}

export default Card;